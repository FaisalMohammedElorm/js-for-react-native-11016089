
// TASK THREE(3)
function createUserProfiles(names, modifiedNames){
  const createUserProfiles = [];
  for(let i = 0; i < names.length; i++){

const name = names[i];
const modifiedName = modifiedNames[i];
const id = i + 1;
userProfiles.push({ originalName: name, modifiedName: id});
  }


return userProfiles;

}